---
title: Get Started
description: The fastest path from "I'd like to try GiftWrapt" to "my family is using it."
---

import { Card, CardGrid, LinkCard, Aside } from '@astrojs/starlight/components'

GiftWrapt runs as a single Docker container plus a Postgres database. Anything S3-compatible handles image storage. There are two supported deployment shapes:

<CardGrid>
	<LinkCard
		title="Self-Host With Docker"
		href="/deploy/self-hosting/"
		description="One docker compose up. Bundled Postgres, optional bundled storage. Runs anywhere."
	/>
	<LinkCard
		title="Hosted (Vercel + Supabase)"
		href="/deploy/hosted/"
		description="Zero-ops. Vercel for the app, Supabase for Postgres and S3 storage."
	/>
</CardGrid>

## The Five-Minute Version

Want to kick the tires before reading any more? This boots a complete stack locally with bundled Postgres + storage:

```bash
git clone https://github.com/shawnphoffman/giftwrapt.git
cd giftwrapt
cp env.example docker/.env
$EDITOR docker/.env        # set POSTGRES_PASSWORD, BETTER_AUTH_SECRET, BETTER_AUTH_URL, CRON_SECRET
docker compose -f docker/compose.selfhost-garage.yaml up -d
```

Open `BETTER_AUTH_URL` in a browser. The first user to sign up is auto-promoted to admin.

<Aside type="note" title="What You'll Need Before This Works">
At a minimum: `POSTGRES_PASSWORD` (any strong random string), `BETTER_AUTH_SECRET` (`openssl rand -hex 32`), `BETTER_AUTH_URL` (the public URL you'll reach the app from), `CRON_SECRET` (`openssl rand -base64 48 | tr -d '/+=' | head -c 48`), and for Garage specifically `GARAGE_RPC_SECRET` and `GARAGE_ADMIN_TOKEN` (each `openssl rand -hex 32`).
</Aside>

## Bare-Minimum Compose

If you'd rather inline the compose file in your own deploy config, here's the smallest possible shape: app + Postgres only. Storage and cron are addons; see [Self-hosting](/deploy/self-hosting/) for the full version.

```yaml
services:
  app:
    image: ghcr.io/shawnphoffman/giftwrapt:latest
    environment:
      DATABASE_URL: postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/giftwrapt
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
    ports:
      - '3000:3000'
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: giftwrapt
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d giftwrapt']
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  postgres_data:
```

This runs but **without image uploads** (no storage configured) and **without scheduled jobs** (no cron sidecar). Both are easy to add - the full bundled compose includes both, plus an MCP option.

## Next

<CardGrid>
	<LinkCard title="Self-Hosting" href="/deploy/self-hosting/" description="The full compose, plus addons for storage, cron, email, and MCP." />
	<LinkCard title="Hosted (Vercel + Supabase)" href="/deploy/hosted/" description="The tried-and-true managed path." />
	<LinkCard title="Environment Variables" href="/configuration/environment-variables/" description="Every var, grouped and commented." />
	<LinkCard title="Screenshots" href="/overview/screenshots/" description="See what you're getting." />
</CardGrid>
